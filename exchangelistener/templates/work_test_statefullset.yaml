apiVersion: apps/v1beta2
kind: StatefulSet
metadata:
  name: {{ template "exchangelistener.fullname" . }}-work-test
spec:
  serviceName: {{ .Values.workService.name }}
  selector:
    matchLabels:
      app: {{ template "exchangelistener.fullname" . }}
      tag: {{ .Values.workService.tag }}
  replicas: {{ .Values.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ template "exchangelistener.name" . }}
        tag: {{ .Values.workService.tag }}
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /api/listeners/status
              port: http
          readinessProbe:
            httpGet:
              path: /api/listeners/status 
              port: http
          env:
            - name: PodName
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: ExchangeListenerRedisHost
              value: redis.external-services:6379
            - name: ExchangeListenerRedisDatabase
              value: "1"
        {{- with .Values.resources }}
          resources:
{{ toYaml . | indent 12 }}
          {{ end }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
